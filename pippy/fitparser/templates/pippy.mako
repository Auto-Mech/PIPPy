${DataSets}			# Record 1 - Training and test data files
${RangeParameter}  ${RefEnergy}			# Record 2 - Range parameter and reference energy for weight func
${NumRanges}  ${EnergyRanges}	# Record 3 - Number of energy ranges and their values
${NumAtoms}				# Record 4 - Number of atoms
${Symbols}  			# Record 5 - Atom types
${AtomGroups}         		# Record 6 - Atom groups
${ReadBasis}  ${FactorOrder} ${TotalOrder}  ${ExpansionType} ${NumChannels}			# Record 7 - Read basis?, xi_factor, xi_total, Expansion type, num of fragment channels
${FragmentGroups}			# Record 8 - Fragment groups for fragment channel 1
